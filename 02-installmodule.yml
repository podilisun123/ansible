- name: install and run the nginx
  hosts: web
  become: yes
  tasks:
  - name: install ngnix
      ansible.builtin.dnf:
        name: nginx
        state: latest
  - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
    