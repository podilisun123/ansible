- name: using inventory variable
  hosts: localhost
  vars:
    wishes: "good morning"
  tasks:
  - name: print inventory variables
    ansible.builtin.debug:
      msg: "Hi {{wishes}} print brothers  names is {{Brname1}} and {{Brname2}}"